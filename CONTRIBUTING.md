# Contributing

Open an issue before adding a new exercise profile. Describe the movement, supported camera view, repetition boundary, expected feedback, and edge cases. Keep pull requests focused and run `npm run lint && npm test` before submission.
